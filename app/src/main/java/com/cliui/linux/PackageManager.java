package com.cliui.linux;

public class PackageManager {
    public String installPackage(String pkg) {
        return "Package: " + pkg + "\n[Install Termux from F-Droid for package management]";
    }
}
